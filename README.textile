h2. Code Snippets Storage

Code snippets storage with tag support and rich search functionality.

"http://code-snippets.heroku.com":http://code-snippets.heroku.com

h2. Hacking

This project uses gem bundler, so you need to install it at first.

<pre><code>$ gem install bundler
$ sudo bundle install --without production</code></pre>
